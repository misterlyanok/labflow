# Leave empty for local mock mode. For GitHub Pages, set the public Render API URL.
VITE_API_URL=https://your-labflow-api.onrender.com

# Backend only: set this to the university JSON schedule endpoint.
UNIVERSITY_SCHEDULE_URL=https://iis.bsuir.by/api/v1/schedule?studentGroup={groupNumber}
